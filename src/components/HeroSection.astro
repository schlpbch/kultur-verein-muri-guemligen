---
import { Image } from 'astro:assets'
import heroImage from '../assets/images/thesparklettes.jpeg'
---

<section class="rounded-md bg-yellow-300 py-10">
  <div class="container mx-auto px-4 sm:px-0 md:px-6">
    <div
      class="flex flex-col items-center justify-between gap-8 py-10 md:flex-row"
    >
      <div class="flex-1 text-center md:text-left">
        <h1
          class="mb-4 text-4xl font-bold tracking-tighter text-gray-900 md:text-5xl lg:text-6xl"
        >
          Kulturverein Muri Gümligen
        </h1>
        <p class="mb-6 text-xl text-gray-700 md:text-2xl">
          Unser neues Programm 2025 ist da!
        </p>

        <a href="#Programm">
          <button
            class="rounded-md bg-orange-600 px-3 py-3 text-lg font-semibold text-white transition-colors hover:bg-gray-800"
          >
            Zum Programm
          </button>
        </a>
      </div>
      <div class="flex-1">
        <Image
          src={heroImage}
          width={512}
          height={512}
          alt="Hero image"
          class="rounded-lg shadow-lg"
          priority
        />
      </div>
    </div>
  </div>
</section>
